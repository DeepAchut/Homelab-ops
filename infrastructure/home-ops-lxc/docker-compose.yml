services:
  homeassistant:
    image: ghcr.io/home-assistant/home-assistant:stable
    container_name: homeassistant
    network_mode: host
    volumes:
      - /mnt/das/homeassistant/config:/config
      - /etc/localtime:/etc/localtime:ro
      - /run/dbus:/run/dbus:ro
    # devices:
    #   - /dev/ttyUSB0:/dev/ttyUSB0  # Uncomment when Zigbee/Matter USB stick is plugged in
    environment:
      - TZ=America/Chicago
    restart: unless-stopped

  mosquitto:
    image: eclipse-mosquitto:latest
    container_name: mosquitto
    volumes:
      - /mnt/das/mosquitto/config:/mosquitto/config
      - /mnt/das/mosquitto/data:/mosquitto/data
      - /mnt/das/mosquitto/log:/mosquitto/log
    ports:
      - "1883:1883"
      - "9001:9001"
    restart: unless-stopped

  nginx-proxy-manager:
    image: jc21/nginx-proxy-manager:latest
    container_name: npm
    volumes:
      - /mnt/das/nginxproxymanager/data:/data
      - /mnt/das/nginxproxymanager/letsencrypt:/etc/letsencrypt
      - /mnt/das/nginxproxymanager/default_site:/app/npm-logs
      - /mnt/das/nginxproxymanager/default_site:/default_site
    ports:
      - "80:80"
      - "443:443"
      - "81:81"
    restart: unless-stopped

  # wireguard:
  #   image: ghcr.io/wg-easy/wg-easy:latest
  #   container_name: wireguard
  #   cap_add:
  #     - NET_ADMIN
  #     - SYS_MODULE
  #   sysctls:
  #     - net.ipv4.ip_forward=1
  #     - net.ipv4.conf.all.src_valid_mark=1
  #   volumes:
  #     - /mnt/das/wireguard/config:/etc/wireguard
  #   ports:
  #     - "51820:51820/udp"
  #     - "51821:51821/tcp"
  #   environment:
  #     - WG_HOST=${WG_HOST}
  #     - PASSWORD_HASH=${WG_PASSWORD_HASH}
  #     - WG_DEFAULT_DNS=${WG_DNS}
  #     - WG_PORT=51820
  #   env_file:
  #     - .env
  #   restart: unless-stopped

  myspeed:
    image: germannewsmaker/myspeed:latest
    container_name: myspeed
    volumes:
      - /mnt/das/myspeed/data:/myspeed/data
    ports:
      - "5216:5216"
    environment:
      - TZ=America/Chicago
    restart: unless-stopped

  upsnap:
    image: ghcr.io/seriousm4x/upsnap:latest
    container_name: upsnap
    network_mode: host
    environment:
      - TZ=America/Chicago
      - UPSNAP_INTERVAL=@every 10s
      - UPSNAP_SCAN_RANGE=192.168.4.0/24
    volumes:
      - /mnt/das/upsnap/data:/app/pb_data
    restart: unless-stopped

  obsidian-livesync:
    image: couchdb:3.5.0
    container_name: obsidian-livesync
    environment:
      - COUCHDB_USER=${COUCHDB_USER}
      - COUCHDB_PASSWORD=${COUCHDB_PASSWORD}
      - PGID=33
      - PUID=33
      - TZ=America/Chicago
    ports:
      - "5984:5984"
    volumes:
      - /mnt/das/nextcloud/data/deep/files/Notes/ObsidianNotes/ObsidianLiveSync/data/couchdb:/opt/couchdb/data
      - /mnt/das/nextcloud/data/deep/files/Notes/ObsidianNotes/ObsidianLiveSync/data/couchdb/etc/local.ini:/opt/couchdb/etc/local.ini
    restart: unless-stopped