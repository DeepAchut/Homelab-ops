apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hermes-family-data
  namespace: hermes-family
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
