apiVersion: v1
kind: Service
metadata:
  name: hermes-family
  namespace: hermes-family
spec:
  type: ClusterIP
  selector:
    app: hermes-family
  ports:
    - port: 8642
      targetPort: 8642
      name: api
