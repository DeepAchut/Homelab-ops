# PLAINTEXT TEMPLATE — committed only as a shape reference. Copy to
# secret.enc.yaml, fill REAL values, then `sops --encrypt --in-place
# secret.enc.yaml` before committing.
#
# DO NOT commit real secrets to this file.
#
# Required keys:
#   GOOGLE_GENERATIVE_AI_API_KEY — Gemini API key from https://aistudio.google.com.
#                                  Picked up automatically by @ai-sdk/google.
#                                  Same value you already have in
#                                  hermes-credentials.GEMINI_API_KEY — reuse it.
#   HERMES_ADMIN_KEY             — API_SERVER_KEY from admin Hermes's secret
#                                  (kubernetes/apps/hermes-agent/secret.enc.yaml).
#   HERMES_FAMILY_KEY            — API_SERVER_KEY from family Hermes's secret
#                                  (kubernetes/apps/hermes-family/secret.enc.yaml).
#
# NOT included (deliberately):
#   ANTHROPIC_API_KEY — Anthropic API is pay-per-token from day one; no free tier.
#                       Use Claude Code on your laptop for Anthropic-only work
#                       per Phase 26 decision.
apiVersion: v1
kind: Secret
metadata:
  name: opencode-credentials
  namespace: opencode
type: Opaque
stringData:
  GOOGLE_GENERATIVE_AI_API_KEY: "AIzaSy..."
  HERMES_ADMIN_KEY:             "a101bf..."
  HERMES_FAMILY_KEY:            "926d6f..."