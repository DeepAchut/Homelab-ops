apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hindsight-data
  namespace: hindsight
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
