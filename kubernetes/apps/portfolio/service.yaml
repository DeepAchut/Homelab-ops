apiVersion: v1
kind: Service
metadata:
  name: portfolio
  namespace: portfolio
  labels: { app: portfolio }
spec:
  type: NodePort
  selector: { app: portfolio }
  ports:
    - { port: 80, targetPort: 8080, nodePort: 30900, name: http }
