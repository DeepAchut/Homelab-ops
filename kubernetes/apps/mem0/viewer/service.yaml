apiVersion: v1
kind: Service
metadata:
  name: mem0-viewer
  namespace: mem0
  labels: { app: mem0-viewer }
spec:
  type: NodePort
  selector: { app: mem0-viewer }
  ports:
    - { port: 8080, targetPort: 8080, nodePort: 30801, name: http }
